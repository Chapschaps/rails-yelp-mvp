<h1>New</h1>

<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.select :category, collection: Restaurant::TYPES %>
  <%= f.submit "add a restaurant", class: "btn btn-primary" %>
<% end %>
